/**
 * Created by Ivan on 29.03.2017.
 */

$(document).ready(function () {
    function closeAlert() {
       $(".alertInfo").hide(200);
        // $(".alertInfo").css({});
    };
    setTimeout(closeAlert, 7000);
});
